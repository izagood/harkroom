-- 에이전트별 **실행 경로** 선택을 걷어낸다(2026-09-18).
--
-- 이 칸은 하네스 어댑터 이설의 임시 손잡이였다(052). 옛 경로를 둔 채 **한 에이전트만** 새
-- 경로로 돌려 보려고 만든 것이고, 그 비교가 끝나면 함께 지우기로 처음부터 적어 뒀다.
--
-- 이설은 끝났다: 새 경로가 기본이 된 뒤 며칠 돌았고, 옛 분기는 이 릴리스에서 사라진다.
-- 고를 것이 하나뿐이면 고르는 칸은 거짓말이 된다 — 화면에 남겨 두면 사람은 그것이 아직
-- 무언가를 바꾼다고 읽는다.
--
-- **데이터를 잃는다는 사실을 적어 둔다.** 이 칸에 `legacy`·`adapters` 를 골라 둔 에이전트가
-- 있어도 그 선택은 사라진다. 잃어도 되는 이유: 가리키던 두 경로 중 하나가 이 릴리스에
-- 더는 없다.
alter table agent_config drop column if exists execution_path;
